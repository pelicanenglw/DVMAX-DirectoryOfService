//ds.PanelAssays.remove();var distinctPanels = ds.IVLSMasterPanels.distinctValues('panel');distinctPanels;for (var i = 0; i < distinctPanels.length; i++) {		var matchedPanels = ds.IVLSMasterPanels.query ('panel == :1', distinctPanels[i]);		for (var j = 0; j < matchedPanels.length; j++) {				var panelAssay = new ds.PanelAssays();				panelAssay.assay = matchedPanels.assay[j];				panelAssay.save();							};	};